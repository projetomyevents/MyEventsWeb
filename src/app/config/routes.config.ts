const basePaths = {
};

const routesNames = {
  home: '',
  erro404: '404'
};

export const RoutesConfig: any = {
  routesNames,
  routes: {
    home: `${routesNames.home}`,
    error404: `${routesNames.erro404}`
  }
};
